{
  "name": "scroll",
  "version": "0.1.0",
  "description": "Wrapper around restic",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "byte-size": "^8.1.1",
    "fast-folder-size": "^2.2.0",
    "yaml": "^2.3.2"
  }
}
